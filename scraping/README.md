# SoK: Literature Collection

Create local environment and install requirements.txt with:  
```bash
$ python3 -m venv .venv
$ source .venv/bin/activate
$ pip install -r requirements.txt
```

In order, execute: `scrape.py`, `merge.py`, `filter.py`. 
